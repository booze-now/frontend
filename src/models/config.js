/**
 * Alapbeállítások
 */

const config = {
    serverUrl: 'http://localhost:8000/api/staff/', // a záró / jel kell a végére
    sidebarOpened: true,
    theme: 1, // 0 sötét, 1 világos
};

export default config;